# Backend the dev server proxies /api to
VITE_API_TARGET=http://localhost:8000

# Public base URLs of the two deployed sites (used when one needs to link to the
# other — e.g. an admin "preview client link" button, or the client → admin
# staff login). Each site is its own domain.
VITE_CLIENT_BASE_URL=https://app.jiwaraloula.com
VITE_ADMIN_BASE_URL=https://admin.jiwaraloula.com
